# Repository Guidelines

开始在这个仓库工作前，先读 [CLAUDE.md](./CLAUDE.md)。

## 归档规则

- `术中自有万钟粟` 只记录工作技术：工具使用、调试方法、工程流程、排障经验、项目实践方法。
- `库中车马多如簇` 记录硬件模块、芯片、器件、`PCB电路` 与硬件原理知识。
- `硬件原器件`、接口、电源、布线、阻抗、隔离、电路拓扑等内容优先放 `库`，不要放 `术`。

## 改动要求

- 新增、移动或重分类笔记时，同步更新相关 `MOC.md`、`README.md` 和文内反向链接。
- 如果 `CLAUDE.md` 与旧索引、旧分类冲突，以 `CLAUDE.md` 为准并顺手修正。
- 如果内容同时涉及工程实践和器件原理，主记录放 `库`，`术` 只保留工作方法或必要引用。
